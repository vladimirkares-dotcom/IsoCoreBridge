<?xml version="1.0" encoding="UTF-8" ?>
<views:MenuPage
    x:Class="IsoCore.App.Views.UsersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:IsoCore.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:IsoCore.App.ViewModels"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- LEFT: list of users -->
        <StackPanel Grid.Column="0"
                    Margin="16"
                    Spacing="8">
            <TextBlock Text="Uživatelé"
                       FontSize="20"
                       FontWeight="SemiBold" />
            <ListView ItemsSource="{Binding Users}"
                      SelectedItem="{Binding SelectedUser, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:UserListItem">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{x:Bind Username}" Width="120" />
                            <TextBlock Text="{x:Bind DisplayName}" Width="180" />
                            <TextBlock Text="{x:Bind RoleDisplayName}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <!-- RIGHT: user detail form -->
        <StackPanel Grid.Column="1"
                    Margin="16"
                    Spacing="8">
            <TextBlock Text="Detail uživatele"
                       FontSize="20"
                       FontWeight="SemiBold" />

            <TextBox Header="Uživatelské jméno"
                     Text="{Binding EditUsername, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Header="Zobrazované jméno"
                     Text="{Binding EditDisplayName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Header="Role"
                     Text="{Binding EditRole, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Header="Login"
                     Text="{Binding EditLogin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Header="Číslo pracovníka"
                     Text="{Binding EditWorkerNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <CheckBox Content="Aktivní"
                      IsChecked="{Binding EditIsActive, Mode=TwoWay}" />
            <TextBox Header="Poznámka"
                     Text="{Binding EditNote, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap" />

            <PasswordBox x:Name="NewPasswordBox"
                         Header="Nové heslo" />
            <PasswordBox x:Name="ConfirmPasswordBox"
                         Header="Potvrzení hesla" />

            <TextBlock Text="{Binding UserFormError}"
                       TextWrapping="Wrap"
                       Margin="0,4,0,0" />

            <StackPanel Orientation="Horizontal"
                        Spacing="8"
                        Margin="0,8,0,0">
                <Button Content="Nový"
                        Click="NewUserButton_Click" />
                <Button Content="Uložit"
                        Click="SaveUserButton_Click" />
                <Button Content="Smazat"
                        Click="DeleteUserButton_Click" />
                <Button Content="Aktivovat / deaktivovat"
                        Click="ToggleActiveButton_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</views:MenuPage>
